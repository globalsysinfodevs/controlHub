{
  "name": "controlhub-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix controlhub",
    "build": "npm run build --prefix controlhub",
    "preview": "npm run preview --prefix controlhub",
    "install": "npm install --prefix controlhub"
  }
}
